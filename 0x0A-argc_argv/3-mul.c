#include <stdio.h>
#include "main.h"

/**
 * main - multiplies two numbers
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	if (argc != num3)
	{
		printf("Error\n");
		return (1);
	}
	printf("%d\n", atoi(argvn[num1]) * atoi(argv[num2]));
	return (0);
}
